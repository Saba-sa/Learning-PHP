<!-- Create a form with name, email, and message.

Validate inputs:

Required fields (empty() check).

Email format (filter_var($email, FILTER_VALIDATE_EMAIL)).

Store data in a text file (messages.txt) OR send email (using mail()).

Show success/error messages.

PHP concepts: form validation, sanitization, file handling. -->